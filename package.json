{
  "name": "pino",
  "version": "0.0.1",
  "description": "",
  "main": "lib/index.js",
  "module": "lib/index.esm.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib"
  ],
  "scripts": {
    "dev": "npx tsc --watch",
    "test": "jest --detectOpenHandles",
    "build": "npx tsc && npm run build:esm && npm run build:cjs",
    "build:esm": "npx esbuild src/index.ts --bundle --target=esnext --minify --tree-shaking=true --format=esm --outfile=lib/index.esm.js",
    "build:cjs": "npx esbuild src/index.ts --bundle --target=esnext --minify --tree-shaking=true --format=cjs --outfile=lib/index.js"
  },
  "keywords": [],
  "author": "jmmaa",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "@types/node": "^20.3.2",
    "esbuild": "^0.20.0",
    "jest": "^29.5.0",
    "make-dir-cli": "^3.1.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.1.6"
  }
}
